import mem as mymem

class myint64():
    def __init__(self, value):
        if (value > mymem.uint32_max)
                raise exception
        self.p = mymem.store_int64(value, None)
        self.s = 8

    getv()
        return (mymem.load_int64(self.p))

    gets()

